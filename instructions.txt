make the environment
python -m venv env

installing packages:
pip install -r requirements.txt

Server start:
uvicorn src.app.main:app --reload --host 127.0.0.1 --port 8000

#running the full pipeline
uvicorn src.api.pipeline_api:app --reload --host 127.0.0.1 --port 8000

Run label sudio:
label-studio

test the pipeline:
python src/test_pipeline.py --image dev-test/stitched/s7.png --model all

train yolo:
# Train with nano model (fastest, good for testing)
>> python src/train_yolo.py --mode train --data data/yolo/dataset.yaml --epochs 300 --model-size l --batch 
4 --imgsz 1280

python src/train_yolo.py --mode train --data data/yolo_augmented/dataset.yaml --epochs 200 --model-size m --batch 8

Running yolo for real test after trained:
python src/train_yolo.py --mode predict --model experiments/microplastic_yolo/weights/best.pt --image "dev-test/stitched/s7.png"

================================================================================
MASK R-CNN TRAINING (Instance Segmentation)
================================================================================

# Train Mask R-CNN (uses pseudo-masks from bounding boxes since polygon annotations are not available)
python src/train_maskrcnn.py --mode train --annotations data/annotations/annotations.json --image-dir data/annotations/images --epochs 50 --batch 8 --img-size 1280

# Resume training from checkpoint
python src/train_maskrcnn.py --mode train --annotations data/annotations/annotations.json --image-dir data/annotations/images --epochs 100 --resume experiments/maskrcnn_latest.pth

# Run inference on an image
python src/train_maskrcnn.py --mode predict --model experiments/maskrcnn_best.pth --image "dev-test/stitched/s7.png" --conf 0.5

# NOTE: For best segmentation accuracy, re-annotate images in Label Studio using 
# Polygon or Brush tools instead of bounding boxes, then export in COCO format.

================================================================================
YOLO + MASK R-CNN PIPELINE (Recommended for Better Accuracy)
================================================================================

# Step 1: Prepare training crops from YOLO detections
python src/train_maskrcnn_crops.py --mode prepare --images "dev-test/stitched" --yolo experiments/microplastic_yolo/weights/best.pt --yolo-conf 0.1

# Step 2: Train Mask R-CNN on cropped detections
python src/train_maskrcnn_crops.py --mode train --epochs 50 --batch 8

# Step 3: Run the complete pipeline on an image
python src/pipeline_inference.py --image "dev-test/stitched/s7.png"

# Run pipeline with custom models
python src/pipeline_inference.py --image "dev-test/stitched/s7.png" --yolo experiments/microplastic_yolo/weights/best.pt --maskrcnn experiments/maskrcnn_crops_best.pth

# Run pipeline with ellipse masks only (skip Mask R-CNN, faster)
python src/pipeline_inference.py --image "dev-test/stitched/s7.png" --no-maskrcnn



#To train yolo crops for Mask R CNN with SAM Model
python src/sam_auto_annotate.py --mode annotate --crops-dir data/crops --sam-model vit_h --sam-checkpoint sam_vit_h_4b8939.pth


# View random samples
python src/sam_auto_annotate.py --mode visualize --crops-dir data/crops_sam --num-samples 20


# Save visualizations to folder
python src/sam_auto_annotate.py --mode visualize --crops-dir data/crops_sam --save-vis data/mask_previews


# Use the new SAM-annotated crops for training
python src/train_maskrcnn_crops.py --mode train --crops-dir data/crops_sam --epochs 50









#*Worked Commands*

#Train yolo
python src/train_yolo.py --mode train --data data/yolo/dataset.yaml --epochs 
50 --model-size l --batch 8 --imgsz 1280

#check Yolo
python src/train_yolo.py --mode predict --model experiments/microplastic_yolo/weights/best.pt --image "dev-test\stitched\s2.png" --conf 0.01




# Full image with all detections + EfficientNet refinement
python src/train_yolo.py --mode predict --model experiments/microplastic_yolo_max_accuracy/weights/best.pt --image "dev-test/stitched/s25-2.png" --effnet experiments/efficientnet_best.pth --no-filter

# Filtered to circular region only (default)
python src/train_yolo.py --mode predict --model experiments/microplastic_yolo_max_accuracy/weights/best.pt --image "dev-test/stitched/s25-2.png" --effnet experiments/efficientnet_best.pth


#Make groundtruth crops from original dataset
python src/train_maskrcnn_crops.py --mode prepare-gt



#yolo with EfficientNet
python src/train_yolo.py --mode predict --model experiments/microplastic_yolo_max_accuracy/weights/best.pt --image "dev-test/stitched/s25-2.png" --effnet experiments/efficientnet_best.pth




# 1. Train EfficientNet classifier (uses same crops as Mask R-CNN)
python src/train_effnet.py --mode train --crops-dir data/crops_gt_sam --epochs 50

# 2. Train Mask R-CNN segmenter
python src/train_maskrcnn_crops.py --mode train --crops-dir data/crops_gt_sam --epochs 100

# 3. Evaluate EfficientNet
python src/train_effnet.py --mode val --crops-dir data/crops_gt_sam

# 4. Classify a single crop
python src/train_effnet.py --mode predict --image "data/crops_gt_sam/images/some_crop.png"






#check with effnet
python src/train_yolo.py --mode predict --model experiments/microplastic_yolo_max_accuracy/weights/best.pt --image "dev-test/stitched/s24.png" --effnet experiments/efficientnet_best.pth  



#Evaluate Accuracy of Yolo
python src/train_yolo.py --mode val --model experiments/microplastic_yolo_max_accuracy/weights/best.pt --data data/yolo/dataset.yaml

================================================================================
DATA AUGMENTATION (CRITICAL FOR SMALL DATASETS)
================================================================================

The enhanced augmentation script creates a new dataset with both original + augmented images.
This is ESSENTIAL for small datasets (< 200 images) to improve accuracy.

# Standard augmentation (10x copies per image, medium severity)
# Input: 50 train images → Output: 550 train images (50 original + 500 augmented)
python src/augment_data.py --input data/yolo --output data/yolo_augmented_enhanced --factor 10 --severity medium

# Heavy augmentation for very small datasets (20x copies, more aggressive transforms)
python src/augment_data.py --input data/yolo --output data/yolo_aug_heavy --factor 20 --severity heavy

# Light augmentation (5x copies, minimal transforms)
python src/augment_data.py --input data/yolo --output data/yolo_aug_light --factor 5 --severity light

# Also augment validation set (useful for very small datasets < 100 total images)
python src/augment_data.py --input data/yolo --output data/yolo_augmented --factor 10 --augment-val --val-factor 3

# After augmentation, train with the new dataset
python src/train_yolo.py --mode train --data data/yolo_augmented_enhanced/dataset.yaml --epochs 300 --model-size m --batch 8 --imgsz 1280

# Key benefits:
# - Includes original data + augmented versions
# - Smart augmentation preserves small objects (fibers)
# - Class-aware statistics
# - 10-20x more effective training data
# - Better generalization and accuracy





#Train EfficientNet
#1 - cropping dataset for the preparation before training effnet (input dataset - yolo-dir, output directory - crops-dir)
python src/train_maskrcnn_crops.py --mode prepare-gt --yolo-dir data/yolo_augmented --crops-dir data/crops_augmented

#2 - training efficientnet
python src/train_effnet.py --mode train --crops-dir data/crops_augmented 
--epochs 50



#Cellpose mask generation
# Full run
venv\Scripts\python.exe src/cellpose_auto_annotate.py --mode annotate --crops-dir data/crops --class-aware

# On CPU explicitly (slower)
venv\Scripts\python.exe src/cellpose_auto_annotate.py --mode annotate --crops-dir data/crops --class-aware --no-gpu




#Running the full pipeline
python src/pipeline_inference.py --image "datasets/stitched/s1.png" --yolo experiments/yolo/best.pt --maskrcnn experiments/maskrcnn/maskrcnn_crops_best.pth --effnet experiments/efficientnet/efficientnet_best.pth --yolo-conf 0.1 --nms-iou 0.3